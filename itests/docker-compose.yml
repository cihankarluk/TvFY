version: "3.8"

services:
  postgres:
    image: postgres:14.2
    container_name: postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: tvfy
      POSTGRES_DB: tvfy
      POSTGRES_PASSWORD: tvfy
